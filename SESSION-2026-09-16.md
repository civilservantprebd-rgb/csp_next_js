# সেশন-লগ — ২০২৬-০৯-১৬ রাত (৮টা – ১০:৪৫)

> **এটা কী:** আজ রাতে দুই প্রকল্পে যা যা বদলানো হলো তার রেকর্ড — কী বদলাল, **কেন**,
> আর কোন প্রমাণে যাচাই করা হলো। অবস্থা/হ্যান্ডওভারের জন্য `PROJECT-STATUS.md` দেখুন;
> এটা তার ইতিহাস-অংশ।

**সেশন-শুরুতে অবস্থা:** `PROJECT-STATUS.md`-এ লেখা ছিল "flutter analyze → No issues
found", "৩৫টি টেস্ট পাস", "এমুলেটর চলবে না"। **তিনটাই তখন আর সত্যি ছিল না** —
প্রতিটি নিজে চালিয়ে মেপে দেখা হয়েছে।

---

## ১. সার্ভার (Next.js) — যা বদলাল

| ফাইল | কী হলো | কেন |
|---|---|---|
| `src/app/api/reads/route.ts` | **নতুন** — `GET`+`PUT` পড়া-চিহ্ন (`student_read_questions`) | ওয়েবে ফিচার ছিল, **REST রুট ছিল না** → অ্যাপ থেকে পড়া-চিহ্ন দেওয়া অসম্ভব ছিল |
| `src/app/api/exams/route.ts` | **নতুন** — সব পরীক্ষার তালিকা (মেটা), সময় অনুযায়ী নতুন আগে | লিডারবোর্ড বাছার জন্য; `exams/live` কেবল চলমান/আসন্ন দেয়, শেষ হওয়া পরীক্ষা দেয় না |
| `src/app/api/leaderboard/route.ts` | **মুছে ফেলা** | রুটটা **পুরোপুরি নকল** ছিল — হার্ড কোড করা নাম/স্কোর ফেরাত। ব্যাকআপ: `D:\DEEPSEEK\fake-leaderboard-route.ts.bak` |
| `src/app/api/profile/route.ts` | ৪০১-এ স্ট্যান্ডার্ড envelope + `TOKEN_INVALID`/`UNAUTHENTICATED` ফারাক | আগে `{"error":"Unauthorized"}` (String) → অ্যাপ সেটাকে "ডেটা" ভেবে **ক্র্যাশ** করত |
| `src/app/api/profile/results/route.ts` | একই envelope ফিক্স + `course` ও `submittedAtMs` ফিল্ড | "সব পরীক্ষার বিস্তারিত" পেজ এখন কোর্স অনুযায়ী সাজানো; বাংলা ফরম্যাট করা তারিখ দিয়ে সাজানো অসম্ভব |
| `src/lib/supabase.ts` | ক্লায়েন্টের `global.fetch`-এ **`cache: "no-store"`** | **আসল বাগ:** Next.js-এর Data Cache Supabase-এর GET ক্যাশ করত → লেখার পরেও পড়া পুরোনো ডেটা ফেরাত |
| `src/app/api/bookmarks/route.ts` | খালি `id` হলে সার্ভার নিজেই ইউনিক id বানায় | **আসল বাগ:** id ইউনিক, খালি `""` দিয়ে দ্বিতীয় insert duplicate-key-এ নীরবে ব্যর্থ হত |
| `src/actions/mistake-actions.ts` | সাময়িক ডায়াগনস্টিক লগ বসানো ও **আবার তুলে নেওয়া** | শিকড় খুঁজতে; শেষে ফাইল আগের অবস্থায় |

**আজকের রাতে ওঠা দুইটা আসল বাগ (বিস্তারিত `PROJECT-STATUS.md` §৫-ঙ):**

1. **Next.js Data Cache + Supabase** — চিহ্ন/বুকমার্ক "সেভ হচ্ছে না", কারণ ছাড়াই।
   ওয়েব অ্যাপে ধরা পড়েনি, কারণ ওখানে `read-store.ts` localStorage-এ আলাদা কপি রাখে
   (UI সাথে সাথে বদলাত, সার্ভারে কিছুই না গেলেও)। API পথে সেই আড়াল নেই।
2. **সারির খালি `id`** — প্রথম insert চলে, দ্বিতীয়বার duplicate-key; অ্যাকশন কেবল
   `false` ফেরায়, তাই ব্যবহারকারী কারণ জানতে পারে না।

সেই সাথে `/api/reads`-এর PUT এখন **সত্যিকারের আগে/পরে গণনা** রিপোর্ট করে
(`added` · `removed` · `totals`), কারণ অ্যাকশনগুলো কেবল বুলিয়ান ফেরায় — আগে সেই
বুলিয়ানকে "১টি যোগ হলো" বলে দেখানো হচ্ছিল, যা বাগটাই লুকিয়ে রেখেছিল।

---

## ২. অ্যাপ (Flutter) — যা বদলাল

| ফাইল | কী হলো |
|---|---|
| `core/network/api_client.dart` | **স্ট্যাটাসই শেষ কথা** — ৪xx/৫xx মানেই ব্যর্থতা; envelope থাকলে সেটার কোড/বার্তা, না থাকলে স্ট্যাটাস থেকে টাইপড কোড |
| `features/profile/profile_screen.dart` | গঠন না মিললে ক্র্যাশ নয়, কারণসহ বার্তা; প্রতিটি আইটেম যাচাই করে পড়া |
| `features/profile/results_grouping.dart` *(নতুন)* | কোর্স-ভিত্তিক গ্রুপিং — খাঁটি Dart, UI-মুক্ত |
| `features/profile/all_results_screen.dart` | তালিকা **কোর্স অনুযায়ী**, কোর্সের ভেতরে সময় অনুযায়ী (নতুন আগে) |
| `test/results_grouping_test.dart` *(নতুন)* | ১০টি টেস্ট — সাজানোর নিয়ম |
| `features/leaderboard/exam_grouping.dart` *(নতুন)* | পরীক্ষার তালিকা কোর্স অনুযায়ী + সময় অনুযায়ী |
| `features/leaderboard/leaderboard_hub_screen.dart` *(নতুন)* | লিডারবোর্ড ট্যাব: **কোর্স → পরীক্ষা → মেধা তালিকা** |
| `test/exam_grouping_test.dart` *(নতুন)* | ৬টি টেস্ট |
| ~~`features/leaderboard/global_leaderboard_screen.dart`~~ | **মুছে ফেলা** (নকল গ্লোবাল তালিকা); ব্যাকআপ `D:\DEEPSEEK\old-global-leaderboard-screen.dart.bak` |
| `data/models/question_bank.dart` *(নতুন)* | প্রশ্নব্যাংকের টাইপড মডেল (উত্তর/ব্যাখ্যা সহ), সহনশীল `fromJson` |
| `data/repositories/question_bank_repository.dart` *(নতুন)* | `GET /api/question-bank` — পেজিনেশন |
| `features/question_bank/question_bank_read_screen.dart` *(নতুন)* | পড়ার মোড: **উত্তর আগে লুকানো**, ব্যাখ্যা আলাদা বাটনে, স্ক্রলে পরের পাতা |
| `test/question_bank_model_test.dart` *(নতুন)* | ১১টি টেস্ট — পেলোড-পার্সিং (ভুল উত্তর সবুজ দেখানো আটকাতে) |
| `features/question_bank/question_bank_screen.dart` | টপিকে ট্যাপ → প্রশ্ন পড়া; গ্রুপের নিচে "এই গ্রুপের সব প্রশ্ন পড়ুন" |
| `data/repositories/notebook_repository.dart` *(নতুন)* | ✓ পড়া ও ★ ফেভারিট — সার্ভারে (`/api/reads` · `/api/bookmarks`) |
| `core/providers.dart` | `globalLeaderboardProvider` বাদ → `examsCatalogProvider` · `questionBankRepositoryProvider` · `notebookRepositoryProvider` |
| `router/app_router.dart` | `/question-bank/read?topic=…` রুট; লিডারবোর্ড হাব |

---

## ৩. যাচাই (সব সত্যিই চালানো)

| যাচাই | ফল |
|---|---|
| `flutter analyze` | **০ error** — মোট issue ৩৫ → ২৮ (দুই ধাপে কমেছে) |
| `flutter test` | **৬১ পাস · ১ ফেল** (ফেল করা একটিই পুরোনো `front_page_test`) |
| লাইভ টেস্ট (সার্ভার চালু) | ৫টি skip → **০ skip**, সব পাস |
| আসল ফোনে চালানো | Pixel 6a-তে ইনস্টল ও চালু; কোনো এক্সেপশন নেই |
| `/api/profile` · `/profile/results` | ২০০ (আগে ৪০১-এ ক্র্যাশ করত) |
| `/api/exams` | ২০০ — ১৫টি পরীক্ষা, ২টি কোর্স |
| `/api/question-bank` | ২০০ — পেজ ১/২/৩ (স্ক্রলে পরের পাতা আসছে) |
| `/api/reads` (আসল টোকেন) | ২ → যোগ → **৩ (তালিকায় পাওয়া গেছে)** → মুছে → ২ |
| `/api/bookmarks` (আসল টোকেন) | ০ → যোগ → **১** → মুছে → ০ |

> **ফোনের পর্দা এজেন্ট পড়তে পারে না** — তাই UI-নির্ভর জিনিসগুলো টেস্ট + সার্ভার-লগ +
> এক্সেপশন-অনুপস্থিতি দিয়ে যাচাই করা হয়েছে, এবং নতুন লজিকগুলো খাঁটি-ফাংশনে সরিয়ে
> **আসল টেস্ট** লেখা হয়েছে (১০+৬+১১ = ২৭টি নতুন টেস্ট)।

---

## ৪. এখনো বাকি (বিস্তারিত `PROJECT-STATUS.md` §৬)

1. **স্টার্টআপ-রেস + রিফ্রেশ-রিট্রাই** — লগইন-করা শিক্ষার্থীর প্রথম ঝাঁক কলে পুরোনো
   টোকেন যেতে পারে, আর `TOKEN_INVALID`-এ অ্যাপ নিজে থেকে রিফ্রেশ করে রিট্রাই করে না
   (`validateStatus < 500` থাকায় ৪xx কখনো `onError` ইন্টারসেপ্টরে পৌঁছায় না)।
   **সবচেয়ে বেশি ব্যবহারকারীকে ছোঁবে — পরের কাজ এটাই।**
2. `front_page_test.dart` পুরোনো (টেস্ট-স্যুট লাল);
3. `landing_screen.dart` মৃত কোড + analyze-এর অবশিষ্ট ইস্যু;
4. `/api/profile` · `/api/profile/results` এখনো টাইপড মডেল ছাড়া (কাঁচা Map);
5. ব্র্যান্ড-নাম এক করা (আরোহণ বনাম BCS Pro);
6. Supabase redirect URL + আসল শিক্ষার্থী দিয়ে লগইন-ফ্লো;
7. `FLAG_SECURE` (লাইভ পরীক্ষার হল);
8. release signing (এখনো debug key);
9. কোর্সের এনরোল ও ভিডিও-প্লেয়ার; নোটবুক/নিউজ স্ক্রিন।

---

## ৫. চালানোর নিয়ম (সংক্ষেপে)

```powershell
# ১. API সার্ভার
cd D:\App\bcs-one-nextjs ; npx next dev -p 3100

# ২. USB সেতু (ফোনের জন্য বাধ্যতামূলক)
D:\Android\sdk\platform-tools\adb.exe reverse tcp:3100 tcp:3100

# ৩. অ্যাপ
cd D:\App\bcs-one-flutter
D:\flutter\bin\flutter.bat run -d 25071JEGR13032 `
  --dart-define=BCS_API_BASE_URL=http://127.0.0.1:3100 `
  --dart-define=BCS_SUPABASE_URL=https://braytjbujysjydxbuqhv.supabase.co `
  --dart-define=BCS_SUPABASE_ANON_KEY=<.env.local থেকে>
```

⚠️ `flutter`/`adb` PATH-এ নেই — পূর্ণ পথ দিন। বিস্তারিত: `PROJECT-STATUS.md` §২-§৩।
